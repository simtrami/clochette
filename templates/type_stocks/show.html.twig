{% extends 'base.html.twig' %}

{% block title %}TypeStocks{% endblock %}

{% block body %}
    <h1>TypeStocks</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_stock.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_stock.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_stocks_index') }}">back to list</a>

    <a href="{{ path('type_stocks_edit', {'id': type_stock.id}) }}">edit</a>

    {{ include('type_stocks/_delete_form.html.twig') }}
{% endblock %}
