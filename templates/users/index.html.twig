{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block stylesheet %}
    {{ parent() }}
    {{ encore_entry_link_tags('users') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('users') }}
{% endblock %}

{% block body %}
    <div class="alert alert-light">
        <h1 class="display-4">Gestion des utilisateurs</h1>
    </div>

    <a class="btn btn-success btn-lg btn-block" href="{{ path('users_new') }}"><i class="fas fa-user-plus"></i> Ajouter
        un utilisateur</a>

    <div class="table-responsive mt-3">
        <table id="table" class="table table-striped table-bordered">
            <thead class="thead-dark">
            <tr>
                <th>#</th>
                <th>Nom d'Utilisateur</th>
                <th>Email</th>
                <th>Rôle</th>
                <th class="table-actions"></th>
            </tr>
            </thead>

            <tbody>
            {% for user in users %}
                <tr>
                    <th scope="row">{{ user.id }}</th>
                    <td>{{ user.username }}</td>
                    <td>{{ user.email }}</td>
                    <td>
                        {% for role in user.roles %}
                            {{ role != 'ROLE_USER' ? role|trim('ROLE_', 'left')|replace({'_': ' '})|capitalize : '' }}{% if not loop.last %} {% endif %}
                        {% endfor %}
                    </td>
                    <td class="table-actions">
                        <div class="d-flex">
                            <div class="input-group justify-content-around mx-auto">
                                <a class="btn btn-secondary mt-1 {{ is_granted(expression('"ROLE_SUPER_ADMIN" in roles'))==true ? "" : "disabled" }}"
                                   href="{{ path( 'users_edit', {'id' :user.id} ) }}" data-toggle="tooltip"
                                   data-placement="left" title="Editer {{ user.username }}">
                                    <i class="fa fa-user-cog"></i></a>
                                <a class="btn btn-warning mt-1 {{ is_granted(expression('"ROLE_SUPER_ADMIN" in roles'))==true ? "" : "disabled" }}"
                                   href="{{ path( 'users_toggle', {'id' :user.id} ) }}" data-toggle="tooltip"
                                   data-placement="left"
                                   title="{{ user.isActive==1 ? 'Désactiver' : 'Activer' }} {{ user.username }}"
                                   onclick="return confirm('Êtes-vous sûr de vouloir effectuer cette action ?')">
                                    {{ user.isActive==1 ? '<i class="fas fa-ban"></i>' : '<i class="far fa-arrow-alt-circle-up"></i>' }}</a>
                            </div>
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
